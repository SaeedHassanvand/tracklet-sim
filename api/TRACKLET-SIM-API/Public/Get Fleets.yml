info:
  name: Get Fleets
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/public/fleets"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
