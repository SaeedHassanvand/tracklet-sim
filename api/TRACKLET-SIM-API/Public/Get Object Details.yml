info:
  name: Get Object Details
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/public/fleets/{{fleetId}}/objects/{{objectId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
