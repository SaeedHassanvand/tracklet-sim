info:
  name: Get Object Trajectory
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/public/fleets/{{fleetId}}/objects/{{objectId}}/trajectory"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
